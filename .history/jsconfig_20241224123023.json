/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "example.com", // Add the domain of your external images
      "flagcdn.com", // Example of another external domain
    ],
  },
};

module.exports = nextConfig;
